

<!-- Navbar -->
<nav class="navbar navbar-top navbar-expand-md navbar-dark" id="navbar-main">
  <div class="container-fluid">
    <!-- Brand -->
    <a class="h4 mb-0 text-white text-uppercase d-none d-lg-inline-block" href="javascript:;">
        App Cafe
    </a>

    
    <!-- User -->
    <ul class="navbar-nav align-items-center d-none d-md-flex">
      <li class="nav-item dropdown">
        
          <div class="media align-items-center">

            <%= link_to('javascript:;', :data => { :toggle => 'dropdown' } ,class: 'text-white mr-4') do %>
              <span class="font-weight-bold text-lg">
                <%= Cart.all.count %>
              </span>
              <i class="fa fa-shopping-cart"></i>
            <% end %>

            <div class="dropdown-menu dropdown-menu-arrow dropdown-menu-right dropdown-menu-xl ">
              <!-- Dropdown header -->
              <div class="px-3 py-3">
                  <h1 class="text-muted">
                    <strong class="text-primary">
                        Keranjang
                    </strong>
                  </h1>
              </div>
              <!-- List group -->
              <div class="list-group list-group-flush">
                  <% subtotal = [] %>
                  <% @get_carts.each do |k| %>
                  
                  <a href="#!" class="list-group-item list-group-item-action">
                    <div class="row align-items-center">

                        <div class="col ml--2 px-4">
                          <div class="d-flex justify-content-between align-items-center">
                            <div>
                              <h1 class="mb-0 text-lg text-dark">
                                  <%= "#{k.product.name} - #{k.product.unit.name}" %>
                              </h1>
                            </div>
                            <div class="text-right text-muted">
                              <small>
                                <%  
                                  
                                  subtotal.push(k.qty * k.product.harga)
    
                                %>
                                <%= "#{ number_to_currency(k.qty * k.product.harga, unit: 'Rp ') } " %>
                              </small>
                            </div>
                          </div>
                          <p class="text-sm mb-0">
                            <%= "#{k.qty} X #{ number_to_currency(k.product.harga, unit: 'Rp ') }" %>
                          </p>
                        </div>
                    </div>
                  </a>
                  <% end %>
              </div>
              <div class="pl-4 pr-4 pt-4">
                  <div class="d-flex justify-content-between align-items-center">
                      <div>
                          <h2 class="text-primary">
                              <strong>
                                  Subtotal
                              </strong>
                          </h2>
                      </div>
                      <div class="text-right">
                          <h2 class="text-muted">
                              <strong>

                                <%= "#{ number_to_currency(subtotal.sum,unit: 'Rp ') }"  %>
                              </strong>
                          </h2>
                      </div>
                  </div>
              </div>
              <div class="px-3 py-3">
                  <%= link_to admin_order_payment_path, class: 'text-white text-lg font-weight-bold' do %>
                    <div class="bg-primary text-center p-2">
                        Bayar
                    </div>
                  <% end %>
                  
                  <!-- <a href="#" class="text-white text-lg font-weight-bold">
                      <div class="bg-primary text-center p-2">
                          Bayar
                      </div>
                  </a> -->
              </div>

          </div>


            
          </div>
      
      </li>

      <li class="nav-item dropdown">
        <a class="nav-link pr-0" href="#" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          <div class="media align-items-center">
            <span class="avatar avatar-sm rounded-circle">

              <%= image_tag("man-1.png") %>
            </span>
            <!-- <div class="media-body ml-2 d-none d-lg-block">
                <span class="mb-0 text-sm text-white font-weight-bold">
                  Admin
                </span>
            </div> -->
            <div class="dropdown-menu dropdown-menu-arrow dropdown-menu-right">
              <div class=" dropdown-header noti-title">
                <h6 class="text-overflow text-dark m-0">Welcome ADMIN!</h6>
              </div>
              <a href="javascript:;" class="dropdown-item">
                <i class="fa fa-user"></i>
                <span>My profile</span>
              </a>
              <div class="dropdown-divider"></div>
              <a href="#" onclick="event.preventDefault();document.getElementById('logout-form').submit();" class="dropdown-item">
                  <i class="ni ni-user-run"></i>
                  <span>Logout</span>
              </a>
              <form id="logout-form" action="#" method="POST" style="display: none;">
                  
              </form>
            </div>
          </div>
        </a>
        
      </li>
    </ul>
  </div>
</nav>


